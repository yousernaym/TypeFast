<ContentDialog 
	x:Class="TyperUWP.NewTextDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TyperUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Add new text"
    PrimaryButtonText="Save"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
	CloseButtonText="Cancel"
	Opened="ContentDialog_Opened"
	>

	<StackPanel x:Name="stackPanel" Width="600">
		<FlyoutBase.AttachedFlyout>
			<Flyout x:Name="errorFlyout">
				<TextBlock x:Name="errorText" TextWrapping="Wrap" Text="Title Error"/>
			</Flyout>
		</FlyoutBase.AttachedFlyout>
		<TextBox x:Name="titleTb" Header="Title" Grid.Column="1" MaxLength="50" GotFocus="TitleTb_GotFocus"/>
		<TextBox x:Name="textTb" Header="Text" Grid.Column="1" TextWrapping="Wrap" AcceptsReturn="True" Height="280" MaxLength="10000" TextChanged="TextTb_TextChanged" GotFocus="TextTb_GotFocus" Margin="0 7 0 0"/>
		<TextBlock x:Name="textCharCount"/>
		<CheckBox x:Name="asciiLettersCb" Content="Replace non-ASCII letters (é -> e, š -> s etc.)" Margin="0 5 0 0"/>
	</StackPanel>
</ContentDialog>
